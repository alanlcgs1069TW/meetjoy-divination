/**
 * 四化配色（依序 祿 / 權 / 科 / 忌）
 *
 * 三個顯示面共用，**勿各自複製一份**：
 *   1. 飛化星曜外框   `PalaceCell.tsx`   → `--fx-stroke`
 *   2. 自化箭頭       `FeixingLine.tsx`  → line stroke + path fill
 *   3. 中央卡圖例     `CenterInfo.tsx`   → `.fx-lg`
 * 曾經三處各寫一份字面值，改色時容易漏掉其中一處而靜默走鐘（不會報錯）。
 *
 * 選色限制：
 * - 忌維持紅（`#c0392b`）—— 學會慣例，不動
 * - 權原為桃紅 `#d6336c`，與忌的紅色相僅差約 20°、又同為高彩度暖色，
 *   小字級加細框時難以分辨 → 2026-08-11 改為紫 `#7b2fa0`（色相跳到 280°，與紅系徹底分開）
 * - 避開既有運線層色：大限 #9b6a30 / 流年 #1a7a6b / 流月 #8a4fb0 / 流日 #c43e8e / 流時 #d1671f
 *   （流月同為紫系，但兩者出現位置不同：飛化在本命星外框、流月在 overlay 宮名，實測不混淆）
 */
export const HUA_COLORS = ['#1a8f4c', '#7b2fa0', '#2b5cc4', '#c0392b'] as const;
//                          祿 綠      權 紫       科 藍      忌 紅
